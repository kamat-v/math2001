{
  "name": "Math Reasoning & Proof (Lean)",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "onCreateCommand": "lake exe cache get && (lake build || true)",
  "customizations": {
    "vscode": {
      "extensions": ["leanprover.lean4"],
      "settings": {
        "editor.wordWrap": "on"
      }
    }
  }
}